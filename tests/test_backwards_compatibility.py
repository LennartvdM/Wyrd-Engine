"""Regression tests for legacy command-line entry points."""

import pytest

pytestmark = pytest.mark.skip(
    reason="Fixtures for backwards compatibility tests will be populated in the modular refactor."
)
